'use strict';

(function(module) {
    const playlistView = {};

    playlistView.init = function(ctx, next) {
        console.log('route hit');
        
    }

    
    
    module.playlistView = playlistView;
})(window);